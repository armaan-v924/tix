---
title: tix config
description: Manage the global tix config
sidebar:
  order: 4
tableOfContents:
  maxHeadingLevel: 2
---

{/* Generated by `just docs-cli` from the tix source itself.
    Edits made here are overwritten; change the generator under xtask/. */}

Manage the global tix config

```sh frame="none"
tix config <COMMAND>
```


## `tix config add`

Append a value to a list in the config document

```sh frame="none"
tix config add [OPTIONS] <KEY> <VALUE>
```


### Arguments

| Argument | Description |
| --- | --- |
| `<KEY>` | The `<section>.<key>` path of the list (e.g. `defaults.repositories`) |
| `<VALUE>` | The value to append (parsed as a TOML value when possible, else a string) |

### Options

| Option | Description |
| --- | --- |
| `-f, --force` | Append without confirming, even if the value is already in the list |


## `tix config get`

Read a value from the config document

```sh frame="none"
tix config get <KEY>...
```


### Arguments

| Argument | Description |
| --- | --- |
| `<KEY>...` | The `<section>[.<key>]` path(s) to read (e.g. `defaults.branch_prefix`) |


## `tix config init`

Create the global config interactively

```sh frame="none"
tix config init [OPTIONS]
```


### Options

| Option | Description |
| --- | --- |
| `--force` | Overwrite an existing config file |


## `tix config remove`

Remove a value from a list in the config document

```sh frame="none"
tix config remove <KEY> <VALUE>
```


### Arguments

| Argument | Description |
| --- | --- |
| `<KEY>` | The `<section>.<key>` path of the list (e.g. `defaults.repositories`) |
| `<VALUE>` | The value to remove (parsed as a TOML value when possible, else a string) |


## `tix config set`

Set a value in the config document

```sh frame="none"
tix config set <KEY> <VALUE>
```


### Arguments

| Argument | Description |
| --- | --- |
| `<KEY>` | The `<section>.<key>` path to set (e.g. `defaults.branch_prefix`) |
| `<VALUE>` | The new value (parsed as a TOML value when possible, else a string) |


## `tix config show`

Print the global config document

```sh frame="none"
tix config show
```



## `tix config unset`

Remove a key from the config document

```sh frame="none"
tix config unset <KEY>
```


### Arguments

| Argument | Description |
| --- | --- |
| `<KEY>` | The `<section>.<key>` path to remove (e.g. `defaults.branch_prefix`) |

