---
title: CLI Reference
description: Every tix command and flag, generated from the CLI's own definition.
sidebar:
  order: 1
tableOfContents:
  maxHeadingLevel: 2
---

{/* Generated by `just docs-cli` from the tix source itself.
    Edits made here are overwritten; change the generator under xtask/. */}


Every command `tix` accepts, generated from the CLI's own argument
definition.

## Notation

| Form | Meaning |
| --- | --- |
| `<VALUE>` | Required. |
| `[VALUE]` | Optional. |
| `<VALUE>...` | Repeatable. |

Every command also accepts `-h` / `--help`; `tix --version` prints the
version.

## Selecting a ticket

`tix ticket add`, `remove`, `info`, and `destroy` act on one ticket. With no
flag they discover it by walking up from the working directory for a
`.tix/ticket.toml`, nearest ancestor winning.

`--ticket` skips the walk, and its argument is read by shape:

| You write | Read as | Resolves to |
| --- | --- | --- |
| `JIRA-123` | id | `tickets_directory/JIRA-123` |
| `./JIRA-123` | path | that directory |
| `../other`, `/abs/path`, `.` | path | that directory |

A bare name is **always** an id, so a ticket in your working directory must
be written `./JIRA-123`. Both forms assert *this is a ticket root*; pointing
at a subdirectory is an error.

`tix destroy` also takes the ticket positionally, read the same two ways. It
conflicts with `--ticket`.

## Plugins

Any argument `tix` does not recognize is dispatched to a plugin: `tix deploy`
execs `tix-deploy` from `PATH` with the workspace context forwarded.
See the [plugin specification](../../plugins/specification/).

## Global options

These are accepted by every command, before or after the subcommand.

| Option | Description |
| --- | --- |
| `--config <CONFIG>` | Path to the global config file (overrides TIX_CONFIG_PATH and the platform default) |
| `-o, --output <OUTPUT>` | Output format for commands that support it (also forwarded to plugins as --tix-output)<br />Accepts: `json`, `toml`, `default` |
| `--log-level <LOG_LEVEL>` | Set the log level (trace, debug, info, warn, error) |
| `-v, --verbose` | Set the log level to trace (shorthand for --log-level trace) |
| `-q, --quiet` | Set the log level to warn (shorthand for --log-level warn) |

## Command index

| Command | Description |
| --- | --- |
| [`tix cli completions`](./cli/#tix-cli-completions) | Generate shell completions |
| [`tix cli update`](./cli/#tix-cli-update) | Update tix to the latest release |
| [`tix ticket add`](./ticket/#tix-ticket-add) | Add repository worktrees to a ticket<br />Alias: `tix add` |
| [`tix ticket destroy`](./ticket/#tix-ticket-destroy) | Destroy a ticket: prune its worktrees, delete its directory<br />Alias: `tix destroy` |
| [`tix ticket info`](./ticket/#tix-ticket-info) | Show a ticket's metadata and worktrees<br />Alias: `tix info` |
| [`tix ticket list`](./ticket/#tix-ticket-list) | List all tickets in the tickets directory<br />Alias: `tix list` |
| [`tix ticket remove`](./ticket/#tix-ticket-remove) | Remove a worktree from a ticket without destroying it<br />Alias: `tix remove` |
| [`tix ticket setup`](./ticket/#tix-ticket-setup) | Create a new ticket workspace with worktrees<br />Alias: `tix setup` |
| [`tix config add`](./config/#tix-config-add) | Append a value to a list in the config document |
| [`tix config get`](./config/#tix-config-get) | Read a value from the config document |
| [`tix config init`](./config/#tix-config-init) | Create the global config interactively |
| [`tix config remove`](./config/#tix-config-remove) | Remove a value from a list in the config document |
| [`tix config set`](./config/#tix-config-set) | Set a value in the config document |
| [`tix config show`](./config/#tix-config-show) | Print the global config document |
| [`tix config unset`](./config/#tix-config-unset) | Remove a key from the config document |
| [`tix repo add`](./repo/#tix-repo-add) | Register a repository in config (without cloning) |
| [`tix repo clone`](./repo/#tix-repo-clone) | Clone registered repositories to their code paths |

## Environment

`TIX_CONFIG_PATH` sets the global config location. `--config` beats it, and
it is ignored when set but empty. With neither, the file is read from the
platform config directory — `~/.config/tix/config.toml` on Linux,
`~/Library/Application Support/tix/config.toml` on macOS. Every command
except `tix config init` and `tix cli completions` requires it to exist.

## Exit status

`0` on success, `1` on any error, with `error: <message>` on stderr.
Diagnostics and logs also go to stderr, so `tix list --output json | jq`
stays parseable at any log level.
