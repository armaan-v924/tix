---
title: CLI reference
description: Every tix command and flag, generated from the CLI's own definition.
sidebar:
  order: 1
tableOfContents:
  maxHeadingLevel: 2
---

{/* Generated by `just docs-cli` from the clap definition in tix-cli.
    Edit xtask/src/mdx.rs, not this file. */}


Every command `tix` accepts, generated directly from the CLI's own argument
definition. If a flag exists, it is on this page.

## Notation

| Form | Meaning |
| --- | --- |
| `<VALUE>` | Required. |
| `[VALUE]` | Optional. |
| `<VALUE>...` | Repeatable — pass it more than once, or give several values. |

Every command also accepts `-h` / `--help`; `tix --version` prints the
version.

## Selecting a ticket

`tix ticket add`, `remove`, `info`, and `destroy` operate on one ticket. With
no flag they discover it by walking up from the working directory looking for
a `.tix/ticket.toml`, nearest ancestor winning. The walk follows the logical
working directory rather than resolving symlinks, and stops at a filesystem
boundary.

`--ticket` skips the walk, and its argument is read as one of two forms:

| You write | Read as | Resolves to |
| --- | --- | --- |
| `JIRA-123` | id | `tickets_directory/JIRA-123` |
| `./JIRA-123` | path | that directory, relative to the working directory |
| `../other`, `/abs/path`, `.` | path | that directory |

The rule is that a bare name is **always** an id. A ticket directory sitting
in your working directory must therefore be written `./JIRA-123`, not
`JIRA-123` — otherwise it resolves against the tickets directory instead.

Both forms are assertions, not starting points: pointing `--ticket` at a
subdirectory of a ticket is an error rather than a shorter walk.

`tix destroy` additionally takes the ticket as a positional argument, read
the same two ways — `tix destroy JIRA-123`. It conflicts with `--ticket`:
pass one or the other, not both.

## Plugins

Any argument `tix` does not recognise as a built-in command is dispatched to
a plugin: `tix deploy` execs `tix-deploy` from `PATH`, forwarding the
workspace context. Plugins are listed under their own heading in `tix --help`.
See the [plugin specification](https://github.com/armaan-v924/tix/blob/main/PLUGIN_SPEC.md)
for the contract.

## Global options

These are accepted by every command, before or after the subcommand.

| Option | Description |
| --- | --- |
| `--config <CONFIG>` | Path to the global config file (overrides TIX_CONFIG_PATH and the platform default) |
| `-o, --output <OUTPUT>` | Output format for commands that support it (also forwarded to plugins as --tix-output)<br />Accepts: `json`, `toml`, `default` |
| `--log-level <LOG_LEVEL>` | Set the log level (trace, debug, info, warn, error) |
| `-v, --verbose` | Set the log level to trace (shorthand for --log-level trace) |
| `-q, --quiet` | Set the log level to warn (shorthand for --log-level warn) |

## Command index

| Command | Description |
| --- | --- |
| [`tix cli completions`](/reference/cli/cli/#tix-cli-completions) | Generate shell completions |
| [`tix cli update`](/reference/cli/cli/#tix-cli-update) | Update tix to the latest release |
| [`tix ticket add`](/reference/cli/ticket/#tix-ticket-add) | Add repository worktrees to a ticket<br />Alias: `tix add` |
| [`tix ticket destroy`](/reference/cli/ticket/#tix-ticket-destroy) | Destroy a ticket: prune its worktrees, delete its directory<br />Alias: `tix destroy` |
| [`tix ticket info`](/reference/cli/ticket/#tix-ticket-info) | Show a ticket's metadata and worktrees<br />Alias: `tix info` |
| [`tix ticket list`](/reference/cli/ticket/#tix-ticket-list) | List all tickets in the tickets directory<br />Alias: `tix list` |
| [`tix ticket remove`](/reference/cli/ticket/#tix-ticket-remove) | Remove a worktree from a ticket without destroying it<br />Alias: `tix remove` |
| [`tix ticket setup`](/reference/cli/ticket/#tix-ticket-setup) | Create a new ticket workspace with worktrees<br />Alias: `tix setup` |
| [`tix config get`](/reference/cli/config/#tix-config-get) | Read a value from the `[cli]` section |
| [`tix config init`](/reference/cli/config/#tix-config-init) | Create the global config interactively |
| [`tix config set`](/reference/cli/config/#tix-config-set) | Set a value in the `[cli]` section |
| [`tix config show`](/reference/cli/config/#tix-config-show) | Print the global config document |
| [`tix repo add`](/reference/cli/repo/#tix-repo-add) | Register a repository in config (without cloning) |
| [`tix repo clone`](/reference/cli/repo/#tix-repo-clone) | Clone registered repositories to their code paths |

## Environment

| Variable | Effect |
| --- | --- |
| `TIX_CONFIG_PATH` | Location of the global config file. Beaten by `--config`; ignored when set but empty. |

With neither `--config` nor `TIX_CONFIG_PATH`, the config file is read from
the platform config directory — `~/.config/tix/config.toml` on Linux,
`~/Library/Application Support/tix/config.toml` on macOS. Every command
except `tix config init` and `tix cli completions` requires that file to
exist.

## Exit status

`tix` exits `0` on success and `1` on any error, printing `error: <message>`
to standard error. Diagnostics and log output also go to standard error, so
`tix ticket list --output json | jq` stays parseable regardless of log level.
