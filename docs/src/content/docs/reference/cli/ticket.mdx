---
title: tix ticket
description: Create, inspect, and manage ticket workspaces
sidebar:
  order: 2
tableOfContents:
  maxHeadingLevel: 2
---

{/* Generated by `just docs-cli` from the tix source itself.
    Edits made here are overwritten; change the generator under xtask/. */}

Create, inspect, and manage ticket workspaces

```sh frame="none"
tix ticket <COMMAND>
```


## `tix ticket add`

Add repository worktrees to a ticket

```sh frame="none"
tix ticket add [OPTIONS] <REPO_ALIASES>...
```


Also available as `tix add` — the same command, spelled without the group.

### Arguments

| Argument | Description |
| --- | --- |
| `<REPO_ALIASES>...` | Registered repository aliases to add |

### Options

| Option | Description |
| --- | --- |
| `-t, --ticket <TICKET>` | The ticket to act on: a bare id resolved under the tickets directory, or a path (anything containing a separator, or `.`/`..`). Discovered by walking up from the current directory when omitted |
| `-b, --branch <BRANCH>` | Branch for the new worktree(s); derived from `[defaults]` + ticket context when omitted |


## `tix ticket destroy`

Destroy a ticket: prune its worktrees, delete its directory

```sh frame="none"
tix ticket destroy [OPTIONS] [TICKET]
```


Also available as `tix destroy` — the same command, spelled without the group.

### Arguments

| Argument | Description |
| --- | --- |
| `[TICKET]` | The ticket to destroy (id under the tickets directory, or a path); discovered from cwd when omitted |

### Options

| Option | Description |
| --- | --- |
| `-t, --ticket <TICKET>` | The ticket to act on: a bare id resolved under the tickets directory, or a path (anything containing a separator, or `.`/`..`). Discovered by walking up from the current directory when omitted |
| `-f, --force` | Skip confirmation, force-prune (discarding uncommitted changes), and delete the ticket directory even if pruning fails |


## `tix ticket info`

Show a ticket's metadata and worktrees

```sh frame="none"
tix ticket info [OPTIONS]
```


Also available as `tix info` — the same command, spelled without the group.

### Options

| Option | Description |
| --- | --- |
| `-t, --ticket <TICKET>` | The ticket to act on: a bare id resolved under the tickets directory, or a path (anything containing a separator, or `.`/`..`). Discovered by walking up from the current directory when omitted |
| `-o, --output <OUTPUT>` | Output format<br />Accepts: `json`, `toml`, `default` |


## `tix ticket list`

List all tickets in the tickets directory

```sh frame="none"
tix ticket list [OPTIONS]
```


Also available as `tix list` — the same command, spelled without the group.

### Options

| Option | Description |
| --- | --- |
| `-o, --output <OUTPUT>` | Output format<br />Accepts: `json`, `toml`, `default` |


## `tix ticket remove`

Remove a worktree from a ticket without destroying it

```sh frame="none"
tix ticket remove [OPTIONS] <NAME>...
```


Also available as `tix remove` — the same command, spelled without the group.

### Arguments

| Argument | Description |
| --- | --- |
| `<NAME>...` | Worktree name(s) from the ticket's worktree map (a bare repo alias matches its default worktree) |

### Options

| Option | Description |
| --- | --- |
| `-t, --ticket <TICKET>` | The ticket to act on: a bare id resolved under the tickets directory, or a path (anything containing a separator, or `.`/`..`). Discovered by walking up from the current directory when omitted |


## `tix ticket setup`

Create a new ticket workspace with worktrees

```sh frame="none"
tix ticket setup [OPTIONS] <KEY> [REPO_ALIASES]...
```


Also available as `tix setup` — the same command, spelled without the group.

### Arguments

| Argument | Description |
| --- | --- |
| `<KEY>` | The ticket key, e.g. JIRA-123 (becomes the directory name; no / or \\) |
| `[REPO_ALIASES]...` | Repositories to include (defaults to `[defaults].repositories`) |

### Options

| Option | Description |
| --- | --- |
| `-d, --description <DESCRIPTION>` | Human-readable description; feeds branch name derivation |
| `-a, --all` | Include every registered repository |

