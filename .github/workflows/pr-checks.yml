name: PR Checks

on:
  pull_request:
    branches:
      - "**"

jobs:
  fmt-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Rust
        uses: actions-rust-lang/setup-rust@v1
        with:
          rust-version: stable
      - name: Format check
        run: cargo fmt -- --check
      - name: Tests
        run: cargo test
